import { Card, Page, Text } from "@shopify/polaris";

export function SettingsPage(): JSX.Element {
  return (
    <Page title="Settings">
      <Card>
        <Text as="p">Settings page lands at M-108.</Text>
      </Card>
    </Page>
  );
}
