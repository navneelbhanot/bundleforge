# Session 0058 — volume bundle vertical slice

`tests/slices/volume.test.ts` — $1 off each unit at/beyond qty 5;
qty 8 → $4 off; below threshold gate fails (skipped reason
`min_quantity_not_met`). 2 cases.
