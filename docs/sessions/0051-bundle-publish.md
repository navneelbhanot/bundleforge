# Session 0051 — BundleService.publish

Added `publish(shopId, id)`: sets `status = "active"`. Real Shopify
product+metafields sync (the heavier part of publish) is wired into
the vertical-slice milestones M-056+ once a live store is available.
1 unit test.
