# Session 0010 — Prisma Seed Script

- **Date:** 2026-05-04
- **Milestone(s):** M-010

## What was done

- Wrote `docs/specs/M-010-seed.md`.
- Verified existing `prisma/seed.ts` typechecks (`tsc --noEmit prisma/seed.ts`).
- Extended seed with: a `volume` bundle with three tiered pricing rules,
  and an enterprise `BillingSubscription` row for the dev shop.
- Live `npm run db:seed` deferred until M-014 (no DB available yet).

## Acceptance criteria

- [x] seed.ts compiles standalone.
- [x] 1 shop + 3 bundles (fixed/build_box/volume) + 1 billing sub.
- [x] Boot phase remains green (81 tests).

## Handoff

Next: **M-011 — CI workflow: typecheck**. Add `.github/workflows/ci.yml`
with a `typecheck` job that runs `npm ci && npm run db:generate &&
npm run typecheck` on every push and PR.
