{
  "name": "@mintbundle/checkout-validation",
  "version": "1.0.0",
  "private": true,
  "description": "Checkout-Validation Function — enforces atomic bundle composition at checkout (Plus only).",
  "license": "UNLICENSED",
  "scripts": {
    "build": "shopify app function build",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@shopify/shopify_function": "~2.0.0"
  }
}
