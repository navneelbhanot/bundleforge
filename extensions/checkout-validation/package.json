{
  "name": "@bundleforge/checkout-validation",
  "version": "1.0.0",
  "private": true,
  "description": "Checkout-Validation Function — enforces atomic bundle composition at checkout (Plus only).",
  "license": "UNLICENSED",
  "scripts": {
    "build": "npm run build:bundle && npm run build:wasm",
    "build:bundle": "esbuild src/run.js --bundle --platform=neutral --format=esm --target=es2022 --outfile=dist/bundled.js",
    "build:wasm": "javy compile dist/bundled.js -o dist/function.wasm",
    "clean": "rm -rf dist"
  }
}
